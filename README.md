# TODO

- Docstrings. ADK sends the docstrings to the LLM as part of the prompt (the
  tool description).

- Session service. Stores conversation history. Create a persistent session
  service.

- Runner. Orchestrates the agent execution loop w/ sub-agent delegation.

- Guardrails.
